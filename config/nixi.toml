[server]
host = "127.0.0.1"
port = 8765

[voice]
wake_phrases = [
  "hey nixi",
  "hey nixie",
  "hey nicki",
  "hey nicky",
  "hey nick see",
  "hey next see",
  "hey next thing",
]
model = "tiny.en"
language = "en"
sample_rate = 16000
speech_threshold = 500
silence_ms = 400
speech_start_ms = 150
min_speech_ms = 250
max_utterance_seconds = 8
command_timeout_seconds = 8
calibration_ms = 1000
adaptive_noise_ratio = 2.5
# While Nixi is speaking, require louder, sustained speech before interrupting.
barge_in_threshold_multiplier = 1.6
barge_in_speech_start_ms = 200
# Set this to a PipeWire node id or name to override the default microphone.
microphone_target = ""

[llm]
model = "gemini-3.5-flash-lite"
system_prompt = """You are Nixi, a concise and friendly desktop voice assistant.
Answer naturally for speech and avoid Markdown unless asked.

You have access to tools on the user's Linux computer:

1. **run_command** — Execute any read-only shell command and see its output.
   Use this proactively to answer questions about the system, check configurations,
   open applications (e.g. "firefox", "code"), open URLs (e.g. "open https://twitter.com"),
   list files, check memory/cpu/disk usage, or gather any information.
   Examples: free -h, df -h, ls ~/Documents, top -bn1 | head -20, xdg-open https://example.com

2. **Pre-configured actions** — volume_up, volume_down, mute_audio, set_wallpaper,
   brightness_up, brightness_down, take_screenshot, media_play_pause, media_next, media_prev.
   Use these for specific media/brightness/wallpaper actions.

Rules:
- Always use run_command for system queries, opening apps, or opening websites.
- Use the pre-configured actions for volume/brightness/media/wallpaper controls.
- Be concise in your spoken responses (1-3 sentences max).
- If a command fails, explain what went wrong briefly."""
max_tokens = 1024
thinking_level = "minimal"
timeout_seconds = 30
history_turns = 8
# Allow Vertex AI to ground time-sensitive questions with Google Search.
google_search_enabled = true

[stt]
enabled = true
model = "saaras:v3-realtime"
language = "en-IN"
mode = "transcribe"
stream_type = "fast"
threshold = 0.3
silence_ms = 500
min_speech_ms = 250
timeout_seconds = 15

[tts]
enabled = true
model = "bulbul:v3"
language = "en-IN"
speaker = "shubh"
pace = 1.05
sample_rate = 24000
temperature = 0.6
timeout_seconds = 30

# --- auto-generated by `nixi-server --discover` --- do not edit below this line ---

[actions.set_wallpaper]
# source: /home/shastri/.config/bspwm/bin/WallSelect
description = "Sets the desktop wallpaper dynamically by launching an interactive Rofi wallpaper selection menu."
command = "/home/shastri/.config/bspwm/bin/WallSelect"

[actions.take_screenshot]
# source: /home/shastri/.config/bspwm/bin/ScreenShoTer
description = "Captures a screenshot of the screen. Opens a Rofi menu to select capture modes such as region selection, fullscreen, or monitor-specific capture."
command = "/home/shastri/.config/bspwm/bin/ScreenShoTer"

[actions.volume_up]
# source: extracted from bspwm config by LLM
description = "Raises the system audio volume in fixed increments using the Volume script and pamixer."
command = "/home/shastri/.config/bspwm/bin/Volume --inc"

[actions.volume_down]
# source: extracted from bspwm config by LLM
description = "Lowers the system audio volume in fixed increments using the Volume script and pamixer."
command = "/home/shastri/.config/bspwm/bin/Volume --dec"

[actions.mute_audio]
# source: extracted from bspwm config by LLM
description = "Mutes or unmutes the system audio using the Volume script and pamixer."
command = "/home/shastri/.config/bspwm/bin/Volume --toggle"

[actions.lock_screen]
# source: /home/shastri/.config/bspwm/bin/ScreenLocker
description = "Locks the desktop session."
command = "/home/shastri/.config/bspwm/bin/ScreenLocker"

[actions.open_launcher]
# source: /home/shastri/.config/bspwm/bin/RofiLauncher
description = "Opens the dynamic Rofi application launcher."
command = "/home/shastri/.config/bspwm/bin/RofiLauncher"

[actions.media_play_pause]
# source: extracted from bspwm config by LLM
description = "Plays or pauses media playback across supported players (MPD, Spotify, etc.) via the MediaControl script."
command = "/home/shastri/.config/bspwm/bin/MediaControl --toggle"

[actions.media_next]
# source: extracted from bspwm config by LLM
description = "Skips to the next media track using the MediaControl script."
command = "/home/shastri/.config/bspwm/bin/MediaControl --next"

[actions.media_prev]
# source: extracted from bspwm config by LLM
description = "Goes to the previous media track using the MediaControl script."
command = "/home/shastri/.config/bspwm/bin/MediaControl --previous"

[actions.brightness_up]
# source: extracted from bspwm config by LLM
description = "Increases screen brightness by fixed steps using the Brightness control script and brightnessctl."
command = "/home/shastri/.config/bspwm/bin/Brightness up"

[actions.brightness_down]
# source: extracted from bspwm config by LLM
description = "Decreases screen brightness by fixed steps using the Brightness control script and brightnessctl."
command = "/home/shastri/.config/bspwm/bin/Brightness down"

# --- end auto-generated ---
