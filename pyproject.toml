[project]
name = "nixi"
version = "0.1.0"
description = "Local voice-command assistant for Linux"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "faster-whisper==1.2.1",
  "numpy==2.5.1",
]

[project.scripts]
nixi-popup = "app.popup.launcher:main"
nixi-server = "app.server.main:main"
nixi-voice = "app.voice.main:main"

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "app"
module-root = ""
